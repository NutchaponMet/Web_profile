<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/home.css">
    <title>Home</title>
</head>
<body>
    <nav class="navbar navbar-dark bg-dark py-3">
        <div class="container-md text-center">
            <a class="navbar-brand mb-0 h1 text-light" href="/">
            Dashboad
            </a>
        </div>
    </nav>
    <div class="container-sm text-center min-vw-50">
        <h1 class="py-3">Information</h1>
        <table class="table table-striped">
            <thead class="text-center">
              <tr>
                <th scope="col">ชื่อ</th>
                <th scope="col">นามสกุล</th>
                <th scope="col">อีเมล</th>
                <th scope="col"></th>
                <th scope="col"></th>
            </tr>
        </thead>
        
        <tbody class="text-center">
            <% if(person != null) { %>
                <% for(let[_, value] of Object.entries(person)) { %>
                    <tr >
                        <td><%= value.fname %></td>
                        <td><%= value.lname %></td>
                        <td><%= value.email %></td>
                        <td><a href="/<%= value._id %>" class="btn btn-warning">แก้ไข</a></td>
                        <td><a href="/delete/<%= value._id %>" class="btn btn-danger" onclick="return confirm('คุณต้องการลบข้อมูลหรือไม่')">ลบ</a></td>
                    </tr>
                <% } %>
            <% } %>
        </tbody>
        </table>
        <div class="container-md mt-3 text-center">
            <a href="/form" class="btn btn-primary">กรอกข้อมูล</a>
        </div>
    </div>

</body>
</html>